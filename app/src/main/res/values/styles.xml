<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <!-- Status Bar Color -->
        <item name="android:statusBarColor">@color/immersive_bars</item>
        <!-- Action mode -->
        <!-- transition and overlay-->
        <item name="android:windowAllowEnterTransitionOverlap">true</item>
        <item name="android:windowAllowReturnTransitionOverlap">true</item>
        <item name="android:windowSharedElementsUseOverlay">false</item>
        <item name="android:windowActionBarOverlay">true</item>
        <item name="android:windowActionModeOverlay">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
    </style>

    <!-- Navigatoin drawer STYLE -->
    <style name="AppTheme.NavigationView">
        <item name="android:textColorPrimary">@color/md_white_1000</item>
        <item name="android:textColorSecondary">@color/md_white_1000</item>
        <item name="android:scrollbarThumbVertical">@color/colorTransparent</item>
        <item name="android:listDivider">@color/colorPrimaryDark</item>
    </style>

    <style name="AppTheme.DrawerActivity">
        <item name="android:statusBarColor">@color/immersive_bars</item>
    </style>


</resources>
